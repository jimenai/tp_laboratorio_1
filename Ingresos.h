/** \brief Pide mediante un mensaje un numero entero y retorna el mismo.
 *
 * \param char El mensaje para pedirle al usuario que ingrese un numero
 * \return char El numero ingresado por el usuario
 *
 */
 int pedirNumero(char[]);

 /** \brief Pide mediante un mensaje una letra en minuscula y retorna la misma.
 *
 * \param char El mensaje para pedirle al usuario que ingrese una letra
 * \return char La letra en minuscula ingresada por el usuario
 *
 */
char pedirLetra(char[]);

